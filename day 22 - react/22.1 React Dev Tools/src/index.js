/* Our starting point is the same as the React Props lession but we're separating components even further
The comments will be on respective files. */

import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));
