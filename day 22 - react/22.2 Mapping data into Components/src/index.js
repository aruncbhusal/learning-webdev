/* In this practice challenge, we need to build an emoji dictionary.
Inspired from https://www.emojimeanings.net/list-smileys-people-whatsapp
We don't need to change anything here, but we'll work on App.jsx */

import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));
