/* We need to create a login page which is a login page if user is not logged in,
but if user is, then it displays a h1 instead
Nothing in this file was changed, the starting code was given, all changes are in components */

import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));
