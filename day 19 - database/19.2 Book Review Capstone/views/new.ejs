<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Post Details</title>
</head>
<body>
    <form action="/new" method="post" class="post-form">
        <input type="hidden" name="bookId" value="<%=book.id%>">
        <label for="title">Name of the Book</label>
        <input type="text" name="bookTitle" id="title" value="<%=book.title%>">
        <label for="olid">Open Library ID (optional)</label>
        <input type="text" name="bookOlid" id="olid" value="<%=book.openlib_id%>">
        <label for="author">Name of the Author</label>
        <input type="text" name="bookAuthor" id="author" value="<%=book.author%>">
        <label for="rating">Rating (out of 5)</label>
        <input type="number" name="bookRating" id="rating" value="<%=book.rating%>" min="1" max="5">
        <label for="review">Book Review</label>
        <textarea name="bookReview" id="review"><%=book.review%></textarea>
        <input type="submit" value="Submit">
    </form>
    <script>
        document.querySelector('form').addEventListener('submit', () => {
            const ratingInput = document.querySelector('#rating');
            const val = parseInt(ratingInput.value);
            if (val < 1) {
                ratingInput.value = 1;
            } else if (val > 5) {
                ratingInput.value = 5;
            } else {
                ratingInput.value = Math.round(val);
            }
        })
    </script>
</body>
</html>