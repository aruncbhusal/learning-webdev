//3. Create a Header.jsx component that renders a <header> element
//to show the Keeper App name in an <h1>.

import React from "react";

function Header() {
  return (
    <header>
      <h1>Keeper</h1>
    </header>
  );
}
// Looks like we don't need to put any effort here to style, as it already is styled by tag and not class/ID

export default Header;
